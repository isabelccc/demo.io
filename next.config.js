/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  // For GitHub Pages: set basePath to your repo name, e.g. basePath: '/demo.io'
  // basePath: process.env.NODE_ENV === 'production' ? '/demo.io' : '',
};

export default nextConfig;
